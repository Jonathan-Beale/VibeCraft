# Full path to the claude CLI executable.
claude-path: "C:\\Users\\jonat\\AppData\\Roaming\\npm\\claude.cmd"

# Full path to the hermes CLI executable.
# TODO: replace personal dev path default — /home/brian/... must not be the default hermesPath
hermes-path: "/home/brian/ai/hermes/.venv/bin/hermes"

# AI provider: 'claude' or 'hermes'
ai-provider: "hermes"

# Where the server's plugins folder is (jars are deployed here).
server-plugins-dir: "C:\\Users\\jonat\\minecraft\\server\\plugins\\"

# Path to the restart flag file watched by RestartWatcher.
restart-flag-path: "C:\\Users\\jonat\\minecraft\\server\\restart.flag"

# Path to the VibeCraft project directory itself (used for workspace scanning and project scaffolding).
vibecraft-dir: "C:\\Users\\jonat\\minecraft\\VibeCraft"

# Root of the Minecraft server (build scripts are placed here).
server-dir: "C:\\Users\\jonat\\minecraft\\server"

# Default repo for players who haven't chosen one yet. Leave blank to show the repo picker instead.
default-repo: ""
